version: "3"

services:
  mongo:
    image: mongo:3.6
    restart: on-failure
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"
      
  mongo-express:
    image: mongo-express:latest
    depends_on:
      - mongo
    ports:
      - "8081:8081"

volumes:
  mongo-data: {}